<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Splittrack" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Splittrack" %>">
    <%= stylesheet_link_tag "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%- if user_signed_in? %>
      <meta name="current-user" content="<%= UserSerializer.new current_user_json %>" />
    <% end %>
  </head>
  <body>
    <div class="background-image"></div>
    <%= yield %>
  </body>
</html>
