<!-- You Are Owed screen -->
<nav>
  <a href='javascript:void(null)' class="button back">Back</a>
  <h1>YOU ARE OWED</h1>
</nav>
<article id="owed" class='next'>
  <section>
    <ul class='list' role='list'>
      <% @contacts.each do |contact| %>
        <li class='comp' data-goto=<%= "owed-entries#{contact.id}" %>>
          <div>
            <h3><%= contact.name %></h3>
          </div>
          <aside>
            <h4><%= current_user.owed(contact.to_param) %></h4>
            <span class='nav'></span>
          </aside>
        </li>
      <% end %>
    </ul>
  </section>
</article>
